import { IpSafeDirective } from './ip-safe.directive';

describe('IpSafeDirective', () => {
  it('should create an instance', () => {
    const directive = new IpSafeDirective();
    expect(directive).toBeTruthy();
  });
});
